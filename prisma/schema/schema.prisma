
generator client {
  provider = "prisma-client"
  // Generate the client in `src/generated/prisma` at the repo root
  output   = "../../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}